import 'package:flutter/material.dart';

class AppColors {
  static Color cardBackgroundColor =
      const HSLColor.fromAHSL(1.0, 0, 0, 1.0).toColor();
  static Color largeTextColor =
      const HSLColor.fromAHSL(1, 218, 0.44, 0.22).toColor();
  static Color backgroundColor =
      const HSLColor.fromAHSL(1, 212, 0.45, 0.89).toColor();
  static Color smallTextColor =
      const HSLColor.fromAHSL(1, 220, 0.15, 0.25).toColor();
}
